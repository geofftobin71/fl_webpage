---
label: Header
hide_body: true
fields:
- name: header
  type: field_group
  config: {}
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
    description: Override the page title in the header (optional).
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: The background image for the page header.
  label: Header
- name: brightness
  type: number
  label: Brightness
  description: Brightness of the header image. Allows the header text and menu to
    be more visible.
  default: 0
  required: true
  config:
    min: 10
    max: 100
    step: 10
