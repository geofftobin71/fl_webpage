---
label: Banner Message
hide_body: true
fields:
- type: textarea
  name: message
  label: Message
  description: Message to display below the banner image on each page.
  config:
    wysiwyg: true
    schema:
      format: markdown
pages:
- src/_data/banner_message.json
