---
label: Shop Categories
hide_body: true
fields:
- type: field_group_list
  name: Shop categories
  label: Shop categories
  fields:
  - type: text
    name: name
    label: Name
    config:
      required: true
  - type: textarea
    name: description
    label: Description
  - name: parent
    type: select
    default: []
    config:
      required: false
      options: []
      source:
        type: documents
        section: ''
        file: src/_data/shop_category_names.json
        path: Shop category names
    label: Parent Category
    description: A product from this Category can only be added to the cart if there
      is a product from the Parent Category in the cart already.
  - type: text
    name: variant
    label: Variant Name
    description: If there are multiple variants of a product (eg. size) the product
      page will show a selector :<br><em>Please choose a ..........</em>
  - type: boolean
    name: disabled
    label: Disabled
    description: Disabled Categories won’t appear on the Shop page.
  - type: boolean
    name: delivery
    label: Delivery
    description: Delivery Address will be collected at checkout if there are delivery
      products in the cart.
  - type: boolean
    name: finite
    label: Finite
    description: Finite products track remaining stock. eg. Workshop tickets.
pages:
- src/_data/shop_categories.json
