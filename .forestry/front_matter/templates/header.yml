---
label: Header
hide_body: true
fields:
- name: header
  type: field_group
  config: {}
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
    description: Override the page title on the header.
  - name: brightness
    type: number
    label: Brightness
    description: Brightness of the header image. Allow the header text and menu to
      be more visible.<br>10 - 100
    default: 100
    config:
      min: 10
      max: 100
      step: 10
  label: Header
