<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1.0, width=device-width">
    <meta http-equiv="X-UA-Compatible" content="ie=edge"> 
    <meta name="description" content="{{ description or site.description }}" />
    
    <link rel="stylesheet" href="{{ '/css/reset.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/modular-scale.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/theme.css' | url }}">
    <link rel="stylesheet" href="{{ '/css/layout.css' | url }}">

    <link rel="shortcut icon" href="/favicon.ico">
    <link rel="icon" sizes="16x16 32x32 64x64" href="/favicon.ico">

    {% block head %}
    {% endblock %}
    
  <title>{% if title | length %}{{ title }} | {{ site.name }}{% else %}{{ site.name }} | {{ site.tagline }}{% endif %}</title>
  </head>
  
  <body>
    {% block content %}
      {{ content | safe }}
    {% endblock %}

    {% block foot %}
    {% endblock %}
  </body>
</html>
