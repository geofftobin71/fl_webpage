{% for section in page_sections %}
  {% if section.template == "text-section" %}
    <section>
      <article class="wrapper text-wrapper">
        {% markdown section.text %}
      </article>
    </section>
  {% elif section.template == "hero-section" %}
    <section>
      <div class="cover shadow" style="--min-height:90vh">
        <figure class="background" style="--brightness:{{ section.brightness or '50' }}%">
          {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (section.image | stripVersion) %}
          {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
          {% set src = site.cloudinary_url + "/w_900," + transforms + (section.image | stripVersion) %}
          <noscript><img src="{{ src }}" alt="{{ section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
          <img data-srcset="
          {%- for size in image_sizes -%}
          {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
          {%- endfor -%}
          " sizes="100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
        </figure>
        <article class="bigfonts font-secondary centered wrapper text-wrapper flow text-center text-shadow">
          {% markdown section.text %}
        </article>
      </div>
    </section>
  {% elif section.template == "split-image-text-section" %}
    <section>
      <div class="domino{{ ' swap' if section.swap }}">
        <figure>
          {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (section.image | stripVersion) %}
          {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
          {% set src = site.cloudinary_url + "/w_900," + transforms + (section.image | stripVersion) %}
          <noscript><img class="img-cover" src="{{ src }}" alt="{{ section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
          <img class="img-cover" data-srcset="
          {%- for size in image_sizes -%}
          {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
          {%- endfor -%}
          " sizes="(orientation: landscape) 50vw, 100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
        </figure>
        <div class="center-both">
          <article class="wrapper text-wrapper">
            {% markdown section.text %}
          </article>
        </div>
      </div>
    </section>
  {% elif section.template == "contact-form-section" %}
    <section>
      <div class="wrapper text-wrapper flow">
        <h2 class="heading text-center">{{ section.heading or 'Contact Us' }}</h2>
        <form id="contact-form" class="req-js" action="/php/contact-form-handler.php" method="post">
          <label for="name-input">
            Name
            <input id="name-input" name="name" type="text" placeholder="Your Name">
          </label>
          <label for="email-input">
            Email
            <input id="email-input" name="email" type="email" placeholder="Your Email">
          </label>
          <label for="message-input">
            Message
            <textarea id="message-input" name="message" placeholder="{{ section.message or 'How can we help you?' }}" rows="10"></textarea>
          </label>
          <input id="subject-input" name="subject" type="hidden" value="{{ section.email_subject or 'Flowers Enquiry' }}">
          <input id="heading-input" name="heading" type="hidden" value="{{ section.email_heading or 'Thankyou for contacting Floriade.' }}">
          <input id="banner-input" name="banner" type="hidden" value="{{ section.email_banner or 'https://floriade.co.nz/images/floriade-icon-round-512.png' }}">
          <input id="alt-input" name="alt" type="hidden" value="{{ section.email_alt or page_alt or site.alt }}">
          <input id="gRecaptchaResponse" name="g-recaptcha-response" type="hidden">

          <p class="text-center font-secondary" style="color:red;min-height:1.5em" id="response-message">&nbsp;</p>
          <div class="text-center"><button type="submit">{{ section.button or 'Send Message' }}</button></div>
          <p class="font-size--2 text-center">Protected by reCAPTCHA &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/privacy/">Privacy</a> &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/terms/">Terms</a></p>
        </form>
        <noscript>
          <div class="text-center flow" style="margin-top:1em">
            <p><a href="{{ site.mailto_obf | safe }}?subject={{ section.email_subject or 'Flowers Enquiry' }}" title="Send us a message">{{ site.email_obf | safe }}</a></p>
            <p><a href="tel:{{ site.phone_intl }}" title="Call now">{{ site.phone }}</a></p>
          </div>
        </noscript>
      </div>
    </section>
    {% set js %}
    {% include "js/contact-form-handler.js" %}
    {% endset %}
    <script>
      {{ js | jsmin | safe }}
    </script>
  {% elif section.template == "two-column-section" %}
    <section>
      <div class="wrapper {{ section.wrapper }} two-columns{{ ' swap' if section.swap }}" style="--domino-space:1em;--section-padding:0">
        {% for left_section in section.left_sections %}
          {% if left_section.template == "text-section" %}
            <section>
              <article class="wrapper text-wrapper">
                {% markdown left_section.text %}
              </article>
            </section>
          {% elif left_section.template == "hero-section" %}
            <section>
              <div class="cover shadow" style="--min-height:90vh">
                <figure class="background" style="--brightness:{{ left_section.brightness or '50' }}%">
                  {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (left_section.image | stripVersion) %}
                  {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
                  {% set src = site.cloudinary_url + "/w_900," + transforms + (left_section.image | stripVersion) %}
                  <noscript><img src="{{ src }}" alt="{{ left_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
                  <img data-srcset="
                  {%- for size in image_sizes -%}
                  {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ left_section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
                  {%- endfor -%}
                  " sizes="100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ left_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
                </figure>
                <article class="bigfonts font-secondary centered wrapper text-wrapper flow text-center text-shadow">
                  {% markdown left_section.text %}
                </article>
              </div>
            </section>
          {% elif left_section.template == "split-image-text-section" %}
            <section>
              <div class="domino{{ ' swap' if left_section.swap }}">
                <figure>
                  {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (left_section.image | stripVersion) %}
                  {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
                  {% set src = site.cloudinary_url + "/w_900," + transforms + (left_section.image | stripVersion) %}
                  <noscript><img class="img-cover" src="{{ src }}" alt="{{ left_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
                  <img class="img-cover" data-srcset="
                  {%- for size in image_sizes -%}
                  {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ left_section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
                  {%- endfor -%}
                  " sizes="(orientation: landscape) 50vw, 100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ left_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
                </figure>
                <div class="center-both">
                  <article class="wrapper text-wrapper">
                    {% markdown left_section.text %}
                  </article>
                </div>
              </div>
            </section>
          {% elif left_section.template == "contact-form-section" %}
            <section>
              <div class="wrapper text-wrapper flow">
                <h2 class="heading text-center">{{ left_section.heading or 'Contact Us' }}</h2>
                <form id="contact-form" class="req-js" action="/php/contact-form-handler.php" method="post">
                  <label for="name-input">
                    Name
                    <input id="name-input" name="name" type="text" placeholder="Your Name">
                  </label>
                  <label for="email-input">
                    Email
                    <input id="email-input" name="email" type="email" placeholder="Your Email">
                  </label>
                  <label for="message-input">
                    Message
                    <textarea id="message-input" name="message" placeholder="{{ left_section.message or 'How can we help you?' }}" rows="10"></textarea>
                  </label>
                  <input id="subject-input" name="subject" type="hidden" value="{{ left_section.email_subject or 'Flowers Enquiry' }}">
                  <input id="heading-input" name="heading" type="hidden" value="{{ left_section.email_heading or 'Thankyou for contacting Floriade.' }}">
                  <input id="banner-input" name="banner" type="hidden" value="{{ left_section.email_banner or 'https://floriade.co.nz/images/floriade-icon-round-512.png' }}">
                  <input id="alt-input" name="alt" type="hidden" value="{{ left_section.email_alt or page_alt or site.alt }}">
                  <input id="gRecaptchaResponse" name="g-recaptcha-response" type="hidden">

                  <p class="text-center font-secondary" style="color:red;min-height:1.5em" id="response-message">&nbsp;</p>
                  <div class="text-center"><button type="submit">{{ left_section.button or 'Send Message' }}</button></div>
                  <p class="font-size--2 text-center">Protected by reCAPTCHA &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/privacy/">Privacy</a> &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/terms/">Terms</a></p>
                </form>
                <noscript>
                  <div class="text-center flow" style="margin-top:1em">
                    <p><a href="{{ site.mailto_obf | safe }}?subject={{ left_section.email_subject or 'Flowers Enquiry' }}" title="Send us a message">{{ site.email_obf | safe }}</a></p>
                    <p><a href="tel:{{ site.phone_intl }}" title="Call now">{{ site.phone }}</a></p>
                  </div>
                </noscript>
              </div>
            </section>
            {% set js %}
            {% include "js/contact-form-handler.js" %}
            {% endset %}
            <script>
              {{ js | jsmin | safe }}
            </script>
          {% endif %}
        {% endfor %}
        {% for right_section in section.right_sections %}
          {% if right_section.template == "text-section" %}
            <section>
              <article class="wrapper text-wrapper">
                {% markdown right_section.text %}
              </article>
            </section>
          {% elif right_section.template == "hero-section" %}
            <section>
              <div class="cover shadow" style="--min-height:90vh">
                <figure class="background" style="--brightness:{{ right_section.brightness or '50' }}%">
                  {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (right_section.image | stripVersion) %}
                  {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
                  {% set src = site.cloudinary_url + "/w_900," + transforms + (right_section.image | stripVersion) %}
                  <noscript><img src="{{ src }}" alt="{{ right_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
                  <img data-srcset="
                  {%- for size in image_sizes -%}
                  {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ right_section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
                  {%- endfor -%}
                  " sizes="100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ right_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
                </figure>
                <article class="bigfonts font-secondary centered wrapper text-wrapper flow text-center text-shadow">
                  {% markdown right_section.text %}
                </article>
              </div>
            </section>
          {% elif right_section.template == "split-image-text-section" %}
            <section>
              <div class="domino{{ ' swap' if right_section.swap }}">
                <figure>
                  {% set lqip_path = site.cloudinary_url + "/c_limit,w_64,h_64,f_jpg,e_blur:200/" + (right_section.image | stripVersion) %}
                  {% set transforms = "c_fill,ar_1,q_auto,f_auto,g_auto:subject/" %}
                  {% set src = site.cloudinary_url + "/w_900," + transforms + (right_section.image | stripVersion) %}
                  <noscript><img class="img-cover" src="{{ src }}" alt="{{ right_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" /></noscript>
                  <img class="img-cover" data-srcset="
                  {%- for size in image_sizes -%}
                  {{ site.cloudinary_url }}/w_{{ size }},{{ transforms }}{{ right_section.image | stripVersion }} {{ size }}w{{ ',' if not loop.last }}
                  {%- endfor -%}
                  " sizes="(orientation: landscape) 50vw, 100vw" data-src="{{ src }}" src="{{ site.transgif }}" alt="{{ right_section.alt or page_alt or site.alt }}" style="background-image:url({{ lqip_path | lqip }})" loading="lazy" decoding="async" />
                </figure>
                <div class="center-both">
                  <article class="wrapper text-wrapper">
                    {% markdown right_section.text %}
                  </article>
                </div>
              </div>
            </section>
          {% elif right_section.template == "contact-form-section" %}
            <section>
              <div class="wrapper text-wrapper flow">
                <h2 class="heading text-center">{{ right_section.heading or 'Contact Us' }}</h2>
                <form id="contact-form" class="req-js" action="/php/contact-form-handler.php" method="post">
                  <label for="name-input">
                    Name
                    <input id="name-input" name="name" type="text" placeholder="Your Name">
                  </label>
                  <label for="email-input">
                    Email
                    <input id="email-input" name="email" type="email" placeholder="Your Email">
                  </label>
                  <label for="message-input">
                    Message
                    <textarea id="message-input" name="message" placeholder="{{ right_section.message or 'How can we help you?' }}" rows="10"></textarea>
                  </label>
                  <input id="subject-input" name="subject" type="hidden" value="{{ right_section.email_subject or 'Flowers Enquiry' }}">
                  <input id="heading-input" name="heading" type="hidden" value="{{ right_section.email_heading or 'Thankyou for contacting Floriade.' }}">
                  <input id="banner-input" name="banner" type="hidden" value="{{ right_section.email_banner or 'https://floriade.co.nz/images/floriade-icon-round-512.png' }}">
                  <input id="alt-input" name="alt" type="hidden" value="{{ right_section.email_alt or page_alt or site.alt }}">
                  <input id="gRecaptchaResponse" name="g-recaptcha-response" type="hidden">

                  <p class="text-center font-secondary" style="color:red;min-height:1.5em" id="response-message">&nbsp;</p>
                  <div class="text-center"><button type="submit">{{ right_section.button or 'Send Message' }}</button></div>
                  <p class="font-size--2 text-center">Protected by reCAPTCHA &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/privacy/">Privacy</a> &nbsp;&nbsp;&nbsp;<a href="https://www.google.com/intl/en/policies/terms/">Terms</a></p>
                </form>
                <noscript>
                  <div class="text-center flow" style="margin-top:1em">
                    <p><a href="{{ site.mailto_obf | safe }}?subject={{ right_section.email_subject or 'Flowers Enquiry' }}" title="Send us a message">{{ site.email_obf | safe }}</a></p>
                    <p><a href="tel:{{ site.phone_intl }}" title="Call now">{{ site.phone }}</a></p>
                  </div>
                </noscript>
              </div>
            </section>
            {% set js %}
            {% include "js/contact-form-handler.js" %}
            {% endset %}
            <script>
              {{ js | jsmin | safe }}
            </script>
          {% endif %}
        {% endfor %}
      </div>
    </section>
  {% endif %}
{% endfor %}
