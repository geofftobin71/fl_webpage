---
label: Background
hide_body: true
fields:
- name: background
  type: field_group
  config: {}
  fields:
  - name: color
    type: include
    config: {}
    template: color
    label: Color
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: The background image for the section.
  - name: brightness
    type: number
    label: Brightness
    description: Brightness of the background image. Allow the text to be more visible.
    default: 66
    config:
      min: 0
      max: 100
  label: Background
