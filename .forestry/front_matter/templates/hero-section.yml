---
label: Hero Section
hide_body: true
display_field: text
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- type: text
  name: alt
  label: Alt Text
  description: Image Alt Text.
- name: text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: markdown
  label: Text
- name: brightness
  type: number
  label: Brightness
  description: Brightness of the background image. Allows the text to be more visible.
  default: 50
  required: false
  config:
    min: 10
    max: 100
    step: 10
